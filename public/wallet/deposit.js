const depositFlows={personal:{network:'Tron (TRC20)',stage:'address',amount:1000,id:null},business:{network:'Tron (TRC20)',stage:'address',amount:5000,id:null}};
const businessDeposits=[];
function depositAccount(){return role==='business'?'business':'personal'}
function depositCode(){const account=depositAccount(),d=depositFlows[account];return `DEMO-${account==='business'?'ATLAS':'ROBERTO'}-${d.network.startsWith('Tron')?'TRC20':'ERC20'}-NAO-ENVIAR`}
function depositPage(){const account=depositAccount(),d=depositFlows[account],company=account==='business';return head('Depositar USDT','Adicione saldo à sua wallet pela rede de sua preferência.',`<span class="badge neutral">${company?'Conta business':'Conta pessoal'}</span>`)+`<div class="grid"><section class="card"><div class="section-head"><div class="row"><span class="square">₮</span><div><h2>USDT</h2><p class="meta">Tether · depósito externo</p></div></div><span class="badge neutral">${d.network}</span></div>${d.stage==='address'?`<label class="field">Rede do depósito<select aria-label="Rede do depósito" onchange="depositFlows[depositAccount()].network=this.value;render()"><option ${d.network==='Tron (TRC20)'?'selected':''}>Tron (TRC20)</option><option ${d.network==='Ethereum (ERC20)'?'selected':''}>Ethereum (ERC20)</option></select></label><div class="deposit-owner"><span class="meta">Destino do depósito</span><h3>${company?'Atlas Performance':'Roberto Costa'}</h3><span class="meta">${company?'Saldo business para pagamentos':'Saldo da sua conta pessoal'}</span></div><label class="field">Endereço de depósito · demonstração<input class="deposit-address" readonly value="${depositCode()}" aria-label="Endereço demonstrativo"></label><button class="btn full" onclick="copyDepositCode()">Copiar endereço demonstrativo</button><div class="deposit-warning"><b>Envie apenas USDT pela rede ${d.network}.</b><p>A rede escolhida na origem precisa ser a mesma exibida aqui.</p></div><p class="meta">Este código é fictício e não recebe fundos. No produto final, esta área exibirá o endereço da sua wallet na rede selecionada.</p><div class="divider"></div><form onsubmit="detectDeposit(event)"><div class="eyebrow">Experimentar o fluxo</div><label class="field">Valor do depósito simulado (USDT)<input id="depositAmount" type="number" min="1" max="1000000" step="0.01" value="${d.amount}" required></label><button class="btn primary full">Simular depósito recebido →</button></form>`:d.stage==='confirming'?`<div class="deposit-state"><span class="square">${icon('history')}</span><h2>Depósito identificado</h2><p>${money(d.amount)} USDT · ${d.network}</p>${badge('Aguardando confirmações')}</div><div class="steps"><div class="step active">1. Identificado</div><div class="step active">2. Confirmando</div><div class="step">3. Saldo liberado</div></div><div class="info">Estamos aguardando as confirmações da rede. Este valor ainda não faz parte do saldo disponível.</div><div class="summary-line"><span>Crédito estimado na wallet</span><b>$${money(d.amount)}</b></div><button class="btn primary full" onclick="completeDeposit()">Simular confirmações e liberar saldo</button><p class="meta" style="margin-top:14px">O avanço é manual apenas nesta demonstração.</p>`:`<div class="deposit-state"><div class="success-icon">✓</div><h2>Saldo disponível.</h2><div class="amount">+$${money(d.amount)}</div><p>Seu depósito de ${money(d.amount)} USDT foi confirmado.</p></div><div class="summary-line"><span>Rede utilizada</span><b>${d.network}</b></div><div class="summary-line"><span>Saldo disponível agora</span><b>$${money(company?businessBalance:balance)}</b></div><div class="toolbar"><button class="btn primary" onclick="go('${company?'business':'home'}')">Ver minha wallet</button><button class="btn" onclick="depositFlows[depositAccount()].stage='address';render()">Novo depósito</button></div>`}</section><div><section class="card"><div class="eyebrow">Da sua wallet para sua vida</div><h2>USDT entra. Seu saldo fica pronto.</h2><div class="partner"><span class="square">1</span><div><h3>Escolha a rede</h3><p>Ethereum (ERC20) ou Tron (TRC20).</p></div></div><div class="partner"><span class="square">2</span><div><h3>Envie de uma wallet ou corretora</h3><p>Use o endereço correspondente à rede escolhida.</p></div></div><div class="partner"><span class="square">3</span><div><h3>Receba no seu saldo</h3><p>${company?'Depois da confirmação, use o saldo para pagar seus beneficiários.':'Depois da confirmação, mantenha o saldo ou saque via PIX, USDT ou BTC.'}</p></div></div><div class="soft-note"><b>Como o valor aparece</b><p>O saldo da wallet é exibido em $. Para esta simulação, 1 USDT recebido = $1 de saldo (USD), sem taxa de depósito. Conversão, taxas e número de confirmações são decisões de produto ainda em aberto.</p></div></section><section class="card section"><h2>Depósitos nesta sessão</h2>${depositHistory(company)}</section></div></div>`}
function depositHistory(company){const list=(company?businessDeposits:txs.filter(t=>t.deposit)).slice(0,4);return list.length?list.map(t=>`<div class="settings-row"><div><h3>${money(t.amount)} USDT</h3><p>${t.network}</p></div>${badge(t.status)}</div>`).join(''):'<p class="meta" style="margin-top:18px">Seus depósitos aparecerão aqui, desde a identificação até a liberação do saldo.</p>'}
async function copyDepositCode(){try{await navigator.clipboard.writeText(depositCode());toast('Código demonstrativo copiado. Não envie fundos.')}catch{toast('Selecione e copie o código demonstrativo exibido.')}}
function detectDeposit(e){e.preventDefault();const account=depositAccount(),d=depositFlows[account];if(d.stage!=='address')return;const amount=Number(document.getElementById('depositAmount').value);if(!Number.isFinite(amount)||amount<1||amount>1000000)return;d.amount=amount;d.stage='confirming';d.id='WG-DEP-'+Date.now();const t={id:d.id,name:'Depósito USDT',note:d.network,network:d.network,amount,date:'13 set 2026, agora',type:'in',status:'Aguardando confirmações',method:'USDT · '+d.network,deposit:true};if(account==='business')businessDeposits.unshift(t);else txs.unshift(t);render()}
function completeDeposit(){const account=depositAccount(),d=depositFlows[account];if(d.stage!=='confirming')return;const t=(account==='business'?businessDeposits:txs).find(t=>t.id===d.id);if(!t||t.status==='Concluído')return;t.status='Concluído';d.stage='done';if(account==='business')businessBalance=Math.round((businessBalance+d.amount)*100)/100;else{balance=Math.round(((emptyMode?0:balance)+d.amount)*100)/100;emptyMode=false}render();toast('Depósito simulado confirmado. Saldo atualizado.')}
